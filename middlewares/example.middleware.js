function example (request, response, next) {
    // Business logic
    // --------------
    // --------------
    
    next(); // para que la petición isga avanzando
}

module.exports = {
    example
}